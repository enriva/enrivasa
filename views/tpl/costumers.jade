div(ng-controller='costumersCtrl')
	article(id='index-text',class='sub-content-text margin-fullwith')
		ul.clientes-ul
			li(ng-repeat='costumer in data.costumers.list',class='pull-{{ort($index)}}')
				img.image_li(src="{{costumer.logo}}")
				div.client_name
					| {{costumer.name}}
